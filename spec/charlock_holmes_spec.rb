# frozen_string_literal: true

RSpec.describe CharlockHolmes do
  it "has a version number" do
    expect(CharlockHolmes::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(true).to eq(true)
  end
end
